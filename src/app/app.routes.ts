import { Routes } from '@angular/router';
import { HomeComponent } from './signin/home/home.component';
import { AppComponent } from './app.component';

export const routes: Routes = [
     {path:'home',component:HomeComponent}
];